name: Build and deploy container to Azure Web App - paa-discordapp-linux

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: paa-discordapp-linux
  RESOURCE_GROUP: Paper-Animal-Adventure
  ACR_NAME: paacontainerregistry
  ACR_LOGIN_SERVER: paacontainerregistry.azurecr.io
  IMAGE_NAME: paa-discordapp

permissions:
  contents: read
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_8D2BC930E1334AE28A149CE9A3EF59B6 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_B0E9855664214AB7AB3B199CD3E8A307 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_02096B9261564C4BB85895514F8B6ADA }}

      - name: Login to ACR
        run: az acr login --name $ACR_NAME

      - name: Build image
        run: |
          docker build \
            -t $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }} \
            -t $ACR_LOGIN_SERVER/$IMAGE_NAME:latest .

      - name: Push image
        run: |
          docker push $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }}
          docker push $ACR_LOGIN_SERVER/$IMAGE_NAME:latest

      - name: Point Web App to new image
        run: |
          az webapp config container set \
            --name $AZURE_WEBAPP_NAME \
            --resource-group $RESOURCE_GROUP \
            --container-image-name $ACR_LOGIN_SERVER/$IMAGE_NAME:${{ github.sha }}

      - name: Restart Web App
        run: |
          az webapp restart \
            --name $AZURE_WEBAPP_NAME \
            --resource-group $RESOURCE_GROUP
